# Legacy SQL dumps

Historical schema dumps and one-off fix scripts that are **not** part of the ordered migration history in `supabase/migrations/`.

Use `supabase/migrations/` for any new schema change.
